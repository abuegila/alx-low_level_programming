#include "main.h"

/**
 * mul - a function that multplies 2 integers
 * @a: the furst integer to be multiplied
 * @b: the second number to be multiplied
 * Return: the reult of multiplication
 */

int mul(int a, int b)
{
return (a * b);
}
