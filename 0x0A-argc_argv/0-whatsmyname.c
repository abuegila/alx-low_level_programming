#include <stdio.h>
/**
 * main - printing the name of the prog
 * @argc: the num or arguments
 * @argv: the array of arguments
 * Return: Always 0
 */
int main(int argc, char const *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
